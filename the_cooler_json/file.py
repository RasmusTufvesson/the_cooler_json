from . import lib as bil
bil[0].k(globals(), "important")
thb = bil
class JSONDecoder(MadeByMe):
    def assign(a,b):
        globals()[a]  =b
    def decode(self):
        JSONDecoder.assign("what", self.a)
        class OkaDecodeITReally(Made):
            def sowhatsit(a):
                tokins = []
                for ca in a.b:
                    if ca == "0": tokins.append(8)
                    else:
                        if ca == "1": tokins.append(7)
                        else:
                            if ca == "3": tokins.append(5)
                            else:
                                if ca == "4": tokins.append(4)
                                else:
                                    if ca == "2": tokins.append(1)
                                    else:
                                        if ca == "5": tokins.append(2)
                                        else:
                                            if ca == "6": tokins.append(3)
                                            else:
                                                if ca == "7": tokins.append(6)
                                                else:
                                                    if ca == "8": tokins.append(0)
                                                    else:
                                                        if ca == "9": tokins.append(9)
                                                        else:
                                                            if ca == '"': tokins.append(23)
                                                            else:
                                                                tokins.append(ord(ca))
                a.a = tokins
            def nowamredty(b):
                thb[1](b.a, self)
        thefing = OkaDecodeITReally()
        thefing.b = self.ted
        return thefing
    def __init_(self, ok):
        import sys, os
        setattr(self, ok, sys.stdout)
        sys.stdout = open(os.devnull, 'w')
    def __exit__(self, a, b, c):
        import sys
        sys.stdout = self.b
    def __enter__(slef):
        slef.__init_("b")
        return slef
    def prinit(fsel, *wasit):
        print(*wasit, file=fsel.b)
    def __init__(e, tod):
        e.__setattr__("ted", tod)
    def result(e):
        return e.__init__
bil = JSONDecoder